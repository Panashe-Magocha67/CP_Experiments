#include<iostream>
using namespace std;
int mmxAND(int arr[], int n)
{
int res=0;
for(int i=0; i<n; i++)
for(int j=0; j<n; j++)
res=max(res,arr[i]& arr[j]);
return res;
}
int main(){
int arr[]={4,8,6,2};
int n=sizeof(arr)/(arr[0]);
count<<"Maximum AND Value="<<maxAND(arr,n);
return 0;
}
