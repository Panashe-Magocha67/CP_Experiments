#include<iostream>
#include<vector>
using namespace std;
int main()
vector<vector<int>>
matrix{{1,2}, {3,4}, {5,6}};
int row=matrix.size();
int col=matrix[0].size();
vector<vector<int>>
transpose(col, vector<int>row(row));
for(int i=0; i<row;i++)
for(int j=0;j<col;j++)
trnspose[i][j]=matrix[i][j];
return0;
